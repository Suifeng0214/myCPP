#include <bits/stdc++.h>
#define int long long 
#define F first
#define S second
using namespace std;
vector<int>a;
unordered_set <int> st;
signed main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	int n;
	while(cin >> n){
		if (n==0) break;
		for (int i = 0; i < n; i++){
			int in;
			cin >> in;
		}
	}
}
