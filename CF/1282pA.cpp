#include <bits/stdc++.h>
using namespace std;
#define int long long
signed main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	int q, a, b, c, r, sum;
	cin >> q;
	while(q--){
		cin >> a >> b >> c >> r;
		if (a > b)
			swqp(a, b);
		 sum = b-a;
		 int x = c - r;
		 int y = c + r;
		 min
		 if ()
	}
	return 0;
} 
